<form method="post" action="{{ path('app_formation_delete', {'id': produit.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette produit ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.id) }}">
    <button class="btn btn-danger btn-sm mt-2"><i class="fa-solid fa-trash"></i></button>
</form>
